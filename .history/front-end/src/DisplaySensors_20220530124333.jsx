import React from 'react'

const Sensors = ({sensor} => {
    const handleDelete = async (id) => {
        await fetch ()
    }
})

const DisplaySensors = () => {
  return (
    <div>DisplaySensors</div>
  )
}

export default DisplaySensors